extends: default

rules:
  # Action versions pinned by commit SHA exceed the default limit
  line-length: disable
  # GitHub Actions requires the "on" key, which YAML 1.1 reads as a boolean
  truthy:
    check-keys: false
